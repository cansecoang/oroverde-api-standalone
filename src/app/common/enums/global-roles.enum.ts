// =========================================================
// 🌐 GLOBAL ROLES (Control Plane)
// =========================================================
export enum GlobalRole {
  SUPER_ADMIN = 'super_admin',
  USER = 'user',
}
